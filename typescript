    export const generateQuestion = action({
      args: {
        topic: v.string(),
        questionType: v.string(),
        difficulty: v.string(),
      },
      handler: async (ctx, args) => {
        // Placeholder for AI question generation
        const mockQuestion = {
          // ... returns a formatted object based on args
        };
        return mockQuestion;
      },
    });
    